specVersion: "v1beta"
serviceId: "ios"
location: "europe-north1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ios-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
